# 公開作業をする人へ（このファイルを最初に読んでください）

このサイトは **2026-09-03 時点で「公開できる状態」まで仕上がっています。**
残っているのは **ドメインに載せる作業と、下の2点の確認だけ** です。

- ドメイン：**kokohoka.com**（取得済み）
- 公開するもの：`main` ブランチの中身をそのまま
- 制作側の担当：りえさん（ディレクター）／制作は Beep Productions 制作部

---

## ⚠️ 確認1：`www` あり／なし、どちらを正にしますか

**いま `https://kokohoka.com`（www なし）を正として書いてあります。**

`www.kokohoka.com` を正にする場合は、**下の4ファイルを書き換えてください**（`kokohoka.com` → `www.kokohoka.com`）。

| ファイル | 箇所 |
|---|---|
| `*.html`（全12ページ） | `<link rel="canonical">` / `og:url` / `og:image` |
| `sitemap.xml` | 12URL すべて |
| `robots.txt` | `Sitemap:` の行 |

一括置換するなら：

```bash
# www を正にする場合のみ実行
grep -rl "https://kokohoka.com" *.html sitemap.xml robots.txt \
  | xargs sed -i '' 's|https://kokohoka\.com|https://www.kokohoka.com|g'
```

**どちらでも構いませんが、必ず片方に統一して、もう片方は 301リダイレクトしてください。**
両方が同じ内容で見えると、検索エンジンが評価を分散させてしまいます。

---

## ⚠️ 確認2：Google Search Console への登録（無料・公開後でOK）

1. Google Search Console に `kokohoka.com` を登録
2. `https://kokohoka.com/sitemap.xml` を送信

新しいドメインなので、これをやるとGoogleの巡回が早まります。
やらなくても最終的には登録されますが、**11月21日・22日のイベント**なので早いほうがいいです。

---

## 補足：アクセス解析について

Beep Productions の Cloudflare アカウントに置くと、**コードを追加しなくてもアクセス解析が使えます**
（Cloudflareが通信の通り道に立っているため）。実際、リポジトリ内に計測タグは1行も入っていません。

プライバシーポリシー（`privacy.html`）は、**特定のサービス名を書かない** 書き方にしてあります。
「アクセス数を記録している」「誰かを特定したり追跡したりしない」という**事実と約束**だけを書いてあるので、
**解析の仕組みが変わっても文章を直す必要はありません。** そのまま公開して大丈夫です。

---

## 触らないでほしいところ

- **`index.html`**（トップページ）… もとは `index-new.html` という名前でした。
  旧トップページは削除済みです。**古い `index.html` を復活させないでください。**
- **`_materials/`** … 大学の年報PDFなど作業用資料。`.gitignore` 済みで公開されません。
- **`images/` の中身** … 未使用画像は整理済み（32MB → 8.6MB）。消したものは git 履歴に残っています。

---

## 公開前チェックの結果（2026-09-03 実施済み・再実施不要）

```
ページ         12枚
内部リンク     全解決・リンク切れ 0
アセット       131件すべて HTTP 200
表示           PC1440 / SP390 実機相当で横スクロール 0件
機密           固有名称・電話番号・メールアドレス・APIキー いずれも検出なし
OGP/favicon    全12ページ設定済み
```

**⚠️ ひとつだけ。児童養護施設の固有名称は、どの成果物にも絶対に出しません。**
公式表現は「社会保護下にある子どもたち」までです。文言を足すときは必ず守ってください。
