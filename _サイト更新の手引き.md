# ここほかサイト 更新の手引き

> **公開後、中身を直すときはこれを読む。** 初回公開の手順は `_公開時の確認事項.md`（別ファイル）。
> 最終更新：2026-09-03

---

## 0. 最初に3つ

1. **ディレクターは りえさん**（代表の妻）。**「Go」が出るまで push しない。** つくるのはOK、世に出すのは判断待ち。
2. **このフォルダはDropboxの外**（`~/Desktop/Code Folder/kokohoka-website`）。Dropboxとgitが衝突して `.git` が壊れるため。
   **着手前に必ず `git pull`。**
3. **デザインの正本はDropbox側にある。** 色・フォント・表記に迷ったら開く：
   `~/Library/CloudStorage/Dropbox/Beep Productions/03_プロジェクト・案件/制作部/2026-001_ここほか_ウェブ・デザイン制作/_ここほかデザイン共通ルール.md`

---

## 1. サイトの全体像（全12ページ）

ドメイン **kokohoka.com**。CSSは `css/kokohoka.css` **一本**。JSは3つ（`main.js` `marquee.js` `typeset.js`）。

| ページ | 中身 | `<body>` のクラス |
|---|---|---|
| `index.html` | トップ | `home` |
| `citizen.html` | 市民の一日 | `p-kid` |
| `oshigoto.html` | お仕事を見てみる | `p-kid` |
| `asobu.html` | あそぶ・まなぶ | `p-kid` |
| `visitor.html` | ビジター（付き添いの大人） | `p-vis` |
| `volunteer.html` | ボランティア募集 | `p-vol` |
| `activity.html` | この活動について | `p-rec` |
| `history.html` | これまでのここほか | `p-rec` |
| `jobs.html` | これまでのお仕事 | `p-rec` |
| `events.html` | これまでの催し・こども大学 | `p-rec` |
| `safeguarding.html` | セーフガーディング指針 | `p-doc` |
| `privacy.html` | 個人情報の取り扱い | `p-rec` |

**`<body>` のクラスがページの系統色を決める。** 新しいページを作るときは必ず付ける。

```
p-kid = コーラル（市民＝こども）   p-vis = 青（ビジター）
p-vol = 緑（ボランティア）          p-rec = マスタード（記録・案内）
p-doc = 濃紺（公式文書。誰向けかではなく「文書」であることを示す）
```

**画像の置き場**

```
images/kv/          キービジュアル（PC/SP × PNG/WebP の4枚）
images/photos/2023  2024  2025   年ごとの記録写真
images/photos/day   市民の一日で使う写真（altに説明文が入っている）
images/photos/top   トップで使う写真
images/frames/      額縁5種（写真に重ねる）
images/motifs/      ⚠ 現在このフォルダの中身は使っていない
```

---

## 2. 触る前に知っておくこと（過去にやらかした罠）

**⚠ フォントは Zen Maru Gothic 一本。** Yomogi・明朝・黒文字は使わない。
2026-09-03、旧デザインの `Darumadrop One` が残っていたので撤去した。**復活させないこと。**

**⚠ `images/motifs/` のうねうねしたモチーフは使用禁止。** 参照を書かない。

**⚠ 子どもへの二人称は「あなた」。「きみ」は使わない。**（りえさん判断＝「あなた」のほうが柔らかい）

**⚠「こどものまち」はすべてひらがな。** 正式名称「こころほかほかあたたかい町」だけ町が漢字。

**⚠ 児童養護施設の固有名称は絶対に出さない。** 公式表現は「社会保護下にある子どもたち」まで。

**⚠ 数字・日程・時間は、大きく載せる前に実行委員長へ確認。**

**⚠ プライバシーポリシー（`privacy.html`）には特定のサービス名を書かない。**
「自分の情報がどこへ行くか」（Googleフォーム等）は名指しする。「どう数えられるか」（解析）は書かない。
**守り続けられない断言をしない**（「Cookieは使っていません」は2026-09-03に削除。理由は下の履歴）。

---

## 3. よくある更新のやり方

### A. 出店するお店・お仕事を追加する（いちばん多いはず）

該当ページ（`oshigoto.html` / `asobu.html` など）の既存カードを**まるごとコピーして中身だけ差し替える**。
新しいHTMLを書き起こさない。既存のクラス構成をそのまま使えば、あしらいは自動で揃う。

### B. 日程・時間・会場を変える

**1ページだけ直して満足しない。** 同じ情報が複数ページとJSON-LDに散っている。必ず全部grepする：

```bash
grep -rn "11月21日\|11月22日\|2026-11-21\|古町7番町\|9:30" *.html
```

`index.html` の `<head>` にある **JSON-LD（構造化データ）の `startDate` / `endDate` / `location`** も忘れずに。
ここは検索結果に日付と会場を出すための記述で、本文には表示されない。

### C. 写真を足す

`images/photos/` の該当フォルダへ。**WebPで**（既存が全部WebP）。
**子どもが写る写真は「掲載OK」のものだけ。**「使用不可」フォルダからは絶対に選ばない。
`alt` には**何をしている場面か**を書く（例：「ハローワークの掲示を見て、やってみたいお仕事をさがす子ども」）。
装飾目的の画像は `alt=""` のままでよい。

### D. ページを新しく作る

既存ページを**コピーして作る**。そのとき必ず引き継ぐもの：

1. `<head>` の meta 一式（description / canonical / og: / favicon / theme-color）
   → **URLとdescriptionを新ページのものに書き換える**
2. `<body class="p-○○">` の系統色
3. **フッターのリンク集**（全12ページで同一。新ページを足したら**全ページのフッターに追加する**）
4. `css/kokohoka.css?v=19` の読み込み（**CSSを直したら `v=` の数字を上げる**。キャッシュ対策）

---

## 4. 直したあとの確認（省略しない）

### ステップ1：静的チェック（数秒・依存なし）

```bash
./_check/verify.sh
```

リンク切れ・画像欠損・meta漏れ・機密情報・デザインルール違反・未使用画像を見る。

### ステップ2：目で見る（**これを飛ばさない**）

```bash
python3 -m http.server 8899
# → http://localhost:8899/ をブラウザで開く
```

**PC幅とスマホ幅の両方**を見る。ブラウザの開発者ツールで幅を390pxにするのが早い。

> ⚠ **`--window-size` を付けただけの headless Chrome のスクリーンショットは信用しないこと。**
> モバイル表示が再現されず、2026-09-03に「スマホで文字が切れている」と誤診した。
> 数値で確かめたいときは CDP で `Emulation.setDeviceMetricsOverride`（`mobile:true`）を使う。

**見るポイント**：横スクロールが出ていないか（`document.documentElement.scrollWidth === innerWidth`）、
文字が枠から出ていないか、画像が抜けていないか。

### ステップ3：報告して、Goをもらう

```
【本番反映前チェック 完了報告】
ゲート1 動作確認：✅/⚠️  ゲート2 機密/セキュリティ：✅/⚠️  ゲート3 クオリティ：✅/⚠️
→ ディレクター（りえさん）の判断をお願いします。
```

**Goが出てから push。**

---

## 5. これまでの経緯（なぜ今こうなっているか）

**2026-09-03　公開準備を完了し、`main` に push（`e809518`）**

- **サイトが2世代同居していた。** 新12ページ（`kokohoka.css`）と旧5ページ（`style.css`／Darumadrop One）が
  互いにリンクせず並存。しかも**新トップの名前が `index-new.html`** だったため、
  **そのまま公開するとドメイン直下で旧サイトが出る状態**だった。旧7枚＋旧CSS＋旧フォントを削除し、
  `index-new.html` → `index.html` にリネーム（参照69箇所を修正）。
- `privacy.html` がどこからもリンクされず孤立していた → 全ページのフッターに追加。
- OGP・favicon（**ブランドの★**）・description・canonical・`robots.txt`・`sitemap.xml` を整備。
- 未参照画像19件を削除（`images/` 32MB → 8.6MB）。
- **検索対応を、見た目を1pxも変えずに実施**：title に「新潟」「こどものまち」を追加／
  トップの `<h2 class="h2">` を `<h1 class="h2">` へ（クラス据え置き。CSSに `h1` のタグ指定が無いことを確認済み）／
  `Event` の JSON-LD を追加。**PC・SPともピクセル差分0を実測。**

**プライバシーポリシーの2つの決定（りえさん判断）**

1. **サービス名を書かない。**「読んでる人もそんなこと詳しく言われてもわからん範囲」。
   法的にも製品名の記載義務はない（求められるのは利用目的の公表）。
2. **「Cookieは使っていません」の断言をやめた。** Cookieを置くか決めるのは実行委員会ではないため、
   設定変更や動画・地図の埋め込み1つで嘘になる。**守れる約束だけを書く**形に変更：
   「だれであるかを特定しない／広告に使わない／他サイトでの行動を追いかけない」。

**アクセス解析について**

Beep Productions の Cloudflare アカウントに載せているため、**計測タグ無しで解析が見える**
（Cloudflareが通信の通り道に立っているから）。リポジトリに計測用のコードは1行も無い。
**新しく解析タグを足す必要はない。**
